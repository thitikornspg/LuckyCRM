LuckyCRM: The Content Intelligence Engine

LuckyCRM คือระบบบริหารจัดการความสัมพันธ์ลูกค้าแบบพิเศษที่ออกแบบมาสำหรับทีม Content Creator โดยเฉพาะ มีจุดประสงค์เพื่อ "ขุดหา" ลูกค้าที่มีดวงดีผิดปกติ (Statistical Anomalies) จากฐานข้อมูลหวย/สลากกินแบ่ง เพื่อนำไปสร้างคอนเทนต์ Viral Marketing หรือสัมภาษณ์ผู้โชคดีได้อย่างรวดเร็วและแม่นยำ

Key Features (ฟีเจอร์หลัก)

Real-time Data Mining: ดึงข้อมูลลูกค้าผู้โชคดีจาก Databricks Data Lakehouse ได้ทันทีที่ผลรางวัลออก โดยไม่ต้องรอ Report แบบเดิมๆ (ลดเวลาจาก 3 วัน เหลือ 3 วินาที)

Smart Segmentation: แบ่งกลุ่มลูกค้าตามพฤติกรรม "ความเฮง" เช่น High Luck (ถูกบ่อย), Newbie (หน้าใหม่ดวงดี), Whale (ซื้อเยอะถูกน้อย)

AI Content Generator: ผสานพลัง Google Gemini AI เพื่อวิเคราะห์โปรไฟล์ลูกค้าและร่างสคริปต์ TikTok/Marketing Strategy ให้ทีมงานนำไปถ่ายทำได้ทันที

Interactive Dashboard: แสดงผลข้อมูลกราฟและตารางที่กรองได้ตามจังหวัด, เพศ, และงวดวันที่ (Round Date)

Cloud Native Architecture: ระบบ Backend ทำงานแบบ Serverless บน Google Cloud Run ทำให้รองรับการขยายตัวได้ดีและประหยัดค่าใช้จ่าย

System Architecture (สถาปัตยกรรมระบบ)

ระบบถูกออกแบบด้วยสถาปัตยกรรมแบบ Decoupled Architecture แยกส่วนหน้าบ้านและหลังบ้านออกจากกันอย่างชัดเจน:

graph LR User[Content Team] -->|View Dashboard| Frontend[React Single Page App] Frontend -->|Fetch JSON Data| CloudRun[Google Cloud Run API] Frontend -->|Generate Content| Gemini[Google Gemini AI] CloudRun -->|SQL Query| Databricks[Databricks SQL Warehouse] Databricks -->|Return Data| CloudRun

Frontend (The Dashboard)
Technology: HTML5, Tailwind CSS, React (via CDN), Chart.js

Role: เป็นหน้า User Interface สำหรับดูภาพรวมและรายชื่อลูกค้า ไฟล์เป็นแบบ Single-file HTML (index.html) เพื่อความง่ายในการโยกย้ายและแก้ไข

Connection: เชื่อมต่อกับ Backend ผ่าน REST API และเชื่อมต่อกับ Google Gemini ผ่าน Client-side API Key

Backend (The API Server)
Technology: Python, FastAPI, Uvicorn

Infrastructure: Docker Container รันบน Google Cloud Run (Region: asia-southeast1)

Role:

รับ Request จาก Frontend

บริหารจัดการ Connection Pool ไปยัง Databricks

จัดการความปลอดภัย (CORS Policy, Environment Variables)

แปลงข้อมูลจาก SQL Row เป็น JSON Format

Database (The Source of Truth)
Technology: Databricks SQL Warehouse

Table: main.silver.dm_lucky_transaction_detail_api_webapp

Logic: ใช้ Window Function (QUALIFY, ROW_NUMBER) เพื่อดึงข้อมูลสถานะล่าสุดของลูกค้าแต่ละคน ลดความซ้ำซ้อนของข้อมูล

Project Structure

โครงสร้างไฟล์ใน Repository:

lucky-crm/ ├── index.html # ไฟล์หน้าบ้าน (Dashboard) รวม HTML/CSS/JS ไว้ในไฟล์เดียว ├── main.py # ไฟล์หลังบ้าน (API Server Logic) เขียนด้วย FastAPI ├── Dockerfile # คำสั่งสำหรับสร้าง Image เพื่อนำขึ้น Cloud Run ├── requirements.txt # รายชื่อ Python Library ที่ต้องใช้ └── README.md # เอกสารฉบับนี้

Getting Started (วิธีการติดตั้งและใช้งาน)

Prerequisites (สิ่งที่ต้องมี)

Google Cloud Platform Account (เปิดใช้งาน Cloud Run และ Container Registry)

Databricks Access (Server Hostname, HTTP Path, และ Access Token)

Google Gemini API Key (สำหรับฟีเจอร์ AI)

Backend Deployment (Google Cloud Run)

เราเลือกใช้ Cloud Run เพราะเป็น Serverless (จ่ายตามจริงเมื่อมีการใช้งาน) และรองรับ Container:

เตรียมไฟล์: ตรวจสอบไฟล์ main.py, requirements.txt, Dockerfile ให้อยู่ในโฟลเดอร์เดียวกัน

เปิด Google Cloud Shell และรันคำสั่งเพื่อ Deploy:

สร้าง Container Image และ Deploy ขึ้น Cloud Run
gcloud run deploy my-api-service
--source .
--platform managed
--region asia-southeast1
--allow-unauthenticated

ตั้งค่า Environment Variables: ไปที่ Cloud Run Console > Edit & Deploy New Revision > Variables & Secrets และเพิ่มค่าดังนี้:

DATABRICKS_SERVER_HOSTNAME: (ค่า Hostname ของ Cluster)

DATABRICKS_HTTP_PATH: (ค่า HTTP Path)

DATABRICKS_TOKEN: (Access Token ของคุณ)

ตรวจสอบ: เมื่อ Deploy เสร็จสิ้น จะได้ URL ปลายทาง เช่น https://my-api-service-xyz.run.app

Frontend Setup

เปิดไฟล์ index.html ด้วย Text Editor

(Optional) แก้ไขตัวแปร defaultUrl ใน JavaScript ให้ชี้ไปที่ Cloud Run URL ของคุณ เพื่อให้ผู้ใช้ไม่ต้องกรอกเองทุกครั้ง

เปิดไฟล์ index.html ผ่าน Browser (Chrome, Edge, Safari) ได้เลย ไม่ต้องติดตั้ง Server เพิ่มเติม

Backend Logic Explanation (main.py)

หัวใจสำคัญของระบบอยู่ที่การ Query ข้อมูลที่มีประสิทธิภาพ:

ตัวอย่าง Query Logic
query = """ SELECT user_id, ... , CASE WHEN total_win_rounds > 20 THEN 'High Luck' ELSE 'General' END as segment FROM main.silver.dm_lucky_transaction_detail_api_webapp WHERE 1=1 -- เทคนิคการตัดข้อมูลซ้ำ: เลือกแถวที่มีวันที่ล่าสุด (round_date) ของแต่ละ user_id QUALIFY ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY round_date DESC) = 1 ORDER BY total_win_rounds DESC LIMIT 500 """

QUALIFY: ใช้เพื่อ De-duplicate ข้อมูล ทำให้ได้ข้อมูลสถานะล่าสุดของลูกค้าแต่ละคนจริงๆ ไม่ใช่ข้อมูลเก่าที่ค้างในระบบ

CORS Middleware: มีการตั้งค่า allow_origins=["*"] เพื่ออนุญาตให้ Frontend (ซึ่งอาจรันจาก Localhost หรือเว็บอื่น) สามารถดึงข้อมูลข้าม Domain ได้

AI Integration Strategy

ระบบใช้ Google gemini-2.5-flash-preview-09-2025 ในการวิเคราะห์ข้อมูล โดยมีการทำ Prompt Engineering ไว้ล่วงหน้า 2 รูปแบบ:

TikTok Viral Key Content: สั่งให้ AI รับบทเป็น Marketing Strategist วิเคราะห์โปรไฟล์ลูกค้าแล้วคิด Hook, Script, และ Visual Idea สำหรับถ่ายทำคลิปสั้น

Expert Marketing Strategy: สั่งให้ AI วิเคราะห์ Pain Point และเสนอแผนการตลาดเพื่อรักษาฐานลูกค้า (Retention)

📝 Author & Acknowledgements

Thitikorn Sapanguen 
Databricks, Google Cloud Platform, and Gemini AI.
